<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.moyada.myblog.admin.dao.AdminDao">
	<!--Admin Login -->
	<select id="getAdmin" resultType="com.moyada.myblog.admin.domain.Admin">
		select * from admin where admin_id=#{admin_id} 
	</select>
</mapper>